/**
 * * -- Insertion Sort --
 * * Time complexity -> O(n^2)
 * * Parameters:
 * * 1) array: number[] -> The array you want to sort
 * * Return:
 * * Sorted array
 */
function insertionSort(array: number[]): number[] {
  // TODO: Change empty array with the sorted array
  return [];
}

export { insertionSort };